[project]
name = "weather_mcp_server_wl"
version = "0.1.0"
description = "给一个地名，然后获取该地方的天气情况"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.14.1",
]
[project.scripts]
weather_mcp_server_wl="weather:main"