[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "prava"
version = "0.1.21"
description = "Prava Python SDK – OpenAI-like API client for LLMs."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [ { name = "Prava AI" } ]
keywords = ["prava", "llm", "sdk", "api"]
dependencies = [
  "requests>=2.27.0"
]

[project.urls]
Homepage = "https://prava.co"
Repository = "https://github.com/pravaco/prava-sdk"

[tool.hatch.build]
include = [
  "prava/**"
]

