[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "materialgui"
version = "0.1.0"
description = "Material Design GUI for Python"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
