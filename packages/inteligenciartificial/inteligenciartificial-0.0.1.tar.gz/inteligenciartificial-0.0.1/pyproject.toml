[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inteligenciartificial"
version = "0.0.1"
description = "Librería de algoritmos de Inteligencia Artificial en Python."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Pedro Shiguihara", email = "p.shiguihara@gmail.com" }]
dependencies = ["numpy>=1.23", "pandas>=1.5"]

[tool.setuptools.packages.find]
where = ["."]
include = ["inteligenciartificial*"]
