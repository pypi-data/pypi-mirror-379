[project]
name = "huang-ssh-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.15.0",
    "paramiko>=4.0.0",
]

[project.scripts]
huang-ssh-demo = "huang_ssh_demo:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
