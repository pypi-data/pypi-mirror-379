[tool.poetry]
name = "aoti"
version = "0.1.0"
description = "Hugging Face ahead-of-time PyTorch compilation"
authors = ["cbensimon <charles@huggingface.co>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
