[build-system]
requires = ["poetry-core>=2.0", "poetry-plugin-freeze"]
build-backend = "poetry.core.masonry.api"


[project]
name = "srum_post_parser"
version = "0.0.0"
description = "Placholder for futur use"
authors = []
license = "MIT"
readme = "README.md"
requires-python = "<4.0,>=3.12"
dependencies = []


[tool.poetry]
packages = [{include = "srum_post_parser", from = "src"}]
