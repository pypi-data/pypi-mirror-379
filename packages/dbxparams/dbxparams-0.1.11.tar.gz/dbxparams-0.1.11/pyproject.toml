[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dbxparams"
version = "0.1.11"
description = "Manage Databricks notebook parameters as Python classes"
authors = [
  { name="Víctor [Tu Apellido]", email="tuemail@example.com" }
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
