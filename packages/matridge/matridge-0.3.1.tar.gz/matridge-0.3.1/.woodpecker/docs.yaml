# DO NOT EDIT
# Generated from .copier-answers.yml

when:
  event: [push, tag, pull_request]
  path: ["matridge/**/*.py", "docs/**/*", "uv.lock", ".woodpecker/docs.yaml"]

steps:
  build:
    image: codeberg.org/${CI_REPO_OWNER}/matridge:ci
    pull: true
    commands:
      - uv sync --all-groups --all-extras
      - cd docs && make html

  publish:
    image: codeberg.org/slidge/woodpecker-publish-pages
    pull: true
    when:
      event: [push, tag]
    settings:
      token:
        from_secret: CODEBERG_TOKEN
