[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "DataValidatorpy"
version = "0.0.1"
description = "una lbreria para validar datos personales y de contacto"
authors = [{name = "Gerardo Burgos"}]
requires-python = ">=3.6"
license = {text = "MIT"}