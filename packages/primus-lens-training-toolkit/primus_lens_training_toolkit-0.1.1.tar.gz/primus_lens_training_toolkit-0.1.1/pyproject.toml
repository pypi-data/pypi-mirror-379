[project]
name = "primus-lens-training-toolkit"
version = "0.1.1"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "wandb>=0.21.4",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"