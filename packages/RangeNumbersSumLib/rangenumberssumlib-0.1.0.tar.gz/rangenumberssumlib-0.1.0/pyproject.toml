[build-system]
requires = ["setuptools>=61.0", "wheel", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "RangeNumbersSumLib"
version = "0.1.0"
description = "Calculate sum from a to b"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name = "Suhail Nabil Alansi", email = "suhail09t12@gmail.com" }]
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
rangesumlibsn = "range_sum_lib.__main__:main"
