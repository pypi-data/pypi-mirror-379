[project]
name = "bloky"
version = "0.1.1"
description = "Analyzes Python code to detect blocking operations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bloky = "bloky:main"

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
