[project]
name = "pypp-python"
version = "1.0.0-alpha"
description = "useful when working on Py++ projects"
authors = [{ name = "Curtis Puetz", email = "pypp.main@yahoo.com" }]
readme = "readme.md"
license = { text = "MIT" }
requires-python = ">=3.13"
urls = { "Homepage" = "https://github.com/curtispuetz/pypp-python" }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"