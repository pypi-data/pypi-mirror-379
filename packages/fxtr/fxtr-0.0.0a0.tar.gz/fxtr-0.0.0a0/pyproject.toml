[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fxtr"
version = "0.0.0a0"
description = ""
requires-python = "==3.12.*,!=3.12.5"
dependencies = []

[tool.uv]
managed = false
