[project]
name = "ag-kit-py"
version = "0.0.1"
description = "ag-kit-py"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "ruff>=0.1.0",
]

[build-system]
requires = ["hatchling>=1.20.0"]
build-backend = "hatchling.build"
