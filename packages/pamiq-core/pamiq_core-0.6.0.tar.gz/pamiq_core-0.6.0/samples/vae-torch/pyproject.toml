[project]
name = "vae-torch"
version = "0.1.0"
description = "Sample VAE project of pamiq-core with pytorch backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.6",
    "pamiq-core",
    "tensorboard>=2.19.0",
    "torch>=2.0.0",
]

[tool.uv.sources]
pamiq-core = { path = "../../" }
