# 默认房间策略：retain（说明：owner 下线后继续保持订阅者连接；可通过命令在运行期调整）
# 未来版本可能支持通过环境变量 DRLMS_DEFAULT_ROOM_POLICY 或配置项进行默认策略控制（当前仅文档说明）。
port: 8080
data_dir: server_files
strict: true
max_conn: 128
rate_up_bps: 0
rate_down_bps: 0
max_upload: 104857600
