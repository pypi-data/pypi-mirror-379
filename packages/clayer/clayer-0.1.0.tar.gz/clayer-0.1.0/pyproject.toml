[project]
name = "clayer"
version = "0.1.0"
description = "Build and run custom data sources for PySpark"
readme = "README.md"
authors = [
    { name = "Daniel Tom", email = "d.e.tom89@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pyspark>=4",
]

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
