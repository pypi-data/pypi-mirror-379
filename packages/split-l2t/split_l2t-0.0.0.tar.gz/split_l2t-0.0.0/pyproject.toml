[build-system]
requires = ["poetry-core>=2.0", "poetry-plugin-freeze"]
build-backend = "poetry.core.masonry.api"


[project]
name = "split_l2t"
version = "0.0.0"
description = "Placholder for futur use"
authors = []
license = "MIT"
readme = "README.md"
requires-python = "<4.0,>=3.12"
dependencies = []


[tool.poetry]
packages = [{include = "split_l2t", from = "src"}]
