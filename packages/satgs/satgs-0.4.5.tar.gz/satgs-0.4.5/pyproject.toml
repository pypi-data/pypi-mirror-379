[project]
name = "satgs"
version = "0.4.5"
description = "A command line tool that controls radios and rotors for amateur satellite operation."
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
authors = [
    {name = "DB8LE",email = "db8le@darc.de"}
]
readme = "README.md"
requires-python = "<4.0,>=3.10"
dependencies = [
    "platformdirs (>=4.3.8,<5.0.0)",
    "requests (>=2.32.4,<3.0.0)",
    "skyfield (>=1.53,<2.0)",
    "importlib-resources (>=6.5.2,<7.0.0)",
    "numpy (>=2.1.0)"
]

[tool.poetry.dependencies]
colorama = { version = ">=0.4.6,<0.5.0", markers = "sys_platform == 'win32'" }

[project.urls]
documentation = "https://satgs.readthedocs.io/en/latest/"
repository = "https://github.com/DB8LE/satgs"
issues = "https://github.com/DB8LE/satgs/issues"

[tool.poetry]
packages = [{ include = "src" }]
include = ["src/resources/**"]

[tool.poetry.scripts]
satgs = "src.main:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.test.dependencies]
nox-poetry = "^1.2.0"
ruff = "^0.12.5"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["I001", "E401", "E501", "E701"]
