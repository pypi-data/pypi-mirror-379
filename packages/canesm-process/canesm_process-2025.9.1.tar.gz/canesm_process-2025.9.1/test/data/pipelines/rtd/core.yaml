setup:
  stages:
    - monthly
    - rtd

# variables in the rtd stage are processed into global, annually averaged variables
rtd:
  reuse: monthly
  variables:
    - GT
    - ST
    - PCP
    - PCPN
    - OLR
    - FSR
    - FSO
    - CLDT
    - PWAT
    - BALT