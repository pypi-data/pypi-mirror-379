[project]
name = "lesson-7-1-plugin"
version = "0.1.3"
description = ""
authors = [
    {name = "Алексей Романов",email = "romanovaleks@ozon.ru"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest (>=8.4.2,<9.0.0)"
]

[tool.poetry.plugins."pytest11"]
    lesson-7-1-plugin = "lesson-7-1-plugin.plugin"

[tool.poetry]
    packages = [{include = "lesson-7-1-plugin", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
