[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "physicslab-coda"
version = "0.2.1"
description = "This library contains the code usefull in physics lab"
authors = [
    { name="Andrea Codarin", email="codarin.andreawork@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = [
    "numpy"
]
