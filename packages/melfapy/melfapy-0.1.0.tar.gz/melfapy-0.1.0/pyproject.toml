[project]
name = "melfapy"
version = "0.1.0"
description = "Python library for controlling Mitsubishi MELFA robots"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.1"
]

[tool.setuptools.packages.find]
where = ["src"]
