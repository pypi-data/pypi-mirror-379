select icu.hadm_id {admission_id}, {attributes}, gcs.charttime {time}
from mimiciv_derived.gcs as gcs
inner join mimiciv_icu.icustays as icu
 on icu.stay_id = gcs.stay_id
where icu.hadm_id is not null
