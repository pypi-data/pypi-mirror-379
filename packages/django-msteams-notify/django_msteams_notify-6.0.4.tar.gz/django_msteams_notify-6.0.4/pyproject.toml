[project]
name = "django-msteams-notify"
version = "6.0.4"
description = "Python Boilerplate contains all the boilerplate you need to create a Python package."
readme = "README.md"
authors = [
  {name = "Asmita Shah", email = "shahasmita379@gmail.com"}
]
maintainers = [
  {name = "Asmita Shah", email = "shahasmita379@gmail.com"}
]
classifiers = [
    # TODO
]
license = {text = "MIT"}
dependencies = [
  "typer"
]
requires-python = ">= 3.10"

[project.optional-dependencies]
test = [
    "coverage",  # testing
    "pytest",  # testing
    "ruff",  # linting
    "ty", # checking types
    "ipdb", # debugging
]

[project.urls]
bugs = "https://github.com/audreyfeldroy/django_msteams_notify/issues"
changelog = "https://github.com/audreyfeldroy/django_msteams_notify/blob/master/changelog.md"
homepage = "https://github.com/audreyfeldroy/django_msteams_notify"

[project.scripts]
django_msteams_notify = "django_msteams_notify.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.uv]
package = true
