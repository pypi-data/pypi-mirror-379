[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "StrictTypePython"
version = "0.1.0"
description = "Python runtime type checking with forceType, forceUnion, remain_forceTypeCheck"
readme = "README.md"
authors = [
    {name = "howShoudIChooseMyUsername", email = "trademark2179@gmail.com"}
]
license = {file = "LICENSE"}
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
