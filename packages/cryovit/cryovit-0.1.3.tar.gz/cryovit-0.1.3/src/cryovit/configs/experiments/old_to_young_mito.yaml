# @package _global_
defaults:
  - override /model: cryovit
  - override /datamodule: multi

name: old_to_young_${hydra:runtime.choices.model}_mito
label_key: mito

datamodule:
  sample:
    - Aged
  test_sample:
    - Young