# @package _global_
defaults:
  - override /model: cryovit
  - override /datamodule: multi

name: young_to_old_${hydra:runtime.choices.model}_mito
label_key: mito

datamodule:
  sample:
    - Young
  test_sample:
    - Aged