name: ${hydra:runtime.choices.datamodule}_any_${hydra:runtime.choices.model}_${label_key}
label_key: ???
additional_keys:
  - data
  # - labels/mito # uncomment to mask results with ground truth mito labels
random_seed: 42

logger: {}

#################### Do not modify #####################

defaults:
  - paths: default
  - model: ???
  - trainer: eval
  - callbacks:
    - rich_progress_bar
    - test_pred_writer  # comment to speed up evaluation if not needed
    - csv_writer
  - datamodule: ???
  - base_experiment_config
  - _self_
  - override hydra/hydra_logging: rich_logger
  - override hydra/job_logging: rich_logger