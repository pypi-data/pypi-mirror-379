_target_: cryovit.datasets.TomoDataset
_partial_: True

input_key: ${model.input_key}
label_key: ${label_key}
split_key: ${datamodule.split_key}
data_root: ${paths.data_dir}/${paths.tomo_name}
aux_keys: ${additional_keys}