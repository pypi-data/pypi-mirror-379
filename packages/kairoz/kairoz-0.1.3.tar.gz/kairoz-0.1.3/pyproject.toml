[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "kairoz"
version = "0.1.3"
authors = [{ name="Felipe Cañas"}, {name="Francisco Giovanni Borgogno"}]
description = "SDK for the Kairoz API"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "requests>=2.0.0",
    "openai>=1.0.0"
]
[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
