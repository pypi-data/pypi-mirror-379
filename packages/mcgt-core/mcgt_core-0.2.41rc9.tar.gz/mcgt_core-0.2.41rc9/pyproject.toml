[project]
license-files = []
version = "0.2.41rc9"
name = "mcgt-core"
readme = "README.md"
dependencies = [
    "numpy>=1.25",
    "scipy>=1.10",
]
[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"
[tool.setuptools.dynamic]
version = { attr = "mcgt.__init__.__version__" }
