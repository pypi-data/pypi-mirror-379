[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qmsolver"
version = "0.1.4"
description = "A quantum mechanics solver library"
authors = [{name = "Georgios Batagiannis", email = "gbatagian@gmail.com"}]
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib"
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Physics",
]

[project.urls]
Homepage = "https://github.com/gbatagian/qmsolver"
Repository = "https://github.com/gbatagian/qmsolver"
Documentation = "https://github.com/gbatagian/qmsolver#readme"

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 80
extend-exclude = '''
/(
  \.venv
  | \.git
  | build
  | dist
)/
'''

[tool.isort]
line_length = 80
force_single_line = true
group_by_package = true
src_paths = ["src"]
profile = "black"
skip_glob = ["**/.venv/**", "**/.git/**", "**/build/**", "**/dist/**"]
