[project]
name = "ainfo"
version = "1.3.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tilman", email = "tk@pm.me" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx",
    "beautifulsoup4",
    "selectolax",
    "pydantic",
    "typer",
    "aiofiles",
    "playwright",
]

[project.scripts]
ainfo = "ainfo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
