[build-system]
requires = ["poetry-core>=2.0", "poetry-plugin-freeze"]
build-backend = "poetry.core.masonry.api"


[project]
name = "kape_helper"
version = "0.0.0"
description = "Placholder for futur use"
authors = []
license = "MIT"
readme = "README.md"
requires-python = "<4.0,>=3.12"
dependencies = []


[tool.poetry]
packages = [{include = "kape_helper", from = "src"}]
